# 规格索引 · 问牍

## 问什么看哪份
- 谁、做不做、成功标准 → `01-PRD.md`（Product Requirements Document / 产品需求文档）
- 用户怎么走完、规则是什么 → `02-产品设计.md`
- 某屏长什么样 → `03-UI交互.md`
- 技术怎么做、前后端怎么连、备选、风险、部署 → `04-技术设计.md`
- 规格改过什么 → `CHANGELOG.md`

## 功能与规则对照
| 功能或规则 | 规格位置 | 代码入口 |
|------------|----------|----------|
| 只根据用户自己的材料回答 | 01#目标 / 02#对象与规则 | `apps/api/app/rag.py` |
| 材料里没有则明确说没有 | 02#对象与规则 / 04#关键流程 | `apps/api/app/rag.py` `ask()` |
| 每条关键结论可点回原文 | 02#对象与规则 / 03 问答页 | `apps/web/src/views/Ask.vue` |
| 登录与按用户隔离；种子管理员；管理员重置密码 | 02#产品层 / 04#API | `apps/api/app/routers_auth.py` / `bootstrap.py` / `accounts.py` |
| 管理页：建用户、LLM Key、改密 | 02#产品层 / 03#admin / 04#API | `apps/api/app/routers_admin.py` / `views/Admin.vue` |
| 上传 PDF/Word/PPT/表格/Markdown/txt；单文件 20MB、无数量上限 | 02#对象与规则 | `apps/api/app/routers_files.py` / `ingest.py` |
| 原文件本地磁盘；浏览器短时签名 PUT | 04#技术栈 / 04#关键流程 | `apps/api/app/storage.py` / `routers_files.py` |
| 实例级 LLM 配置（DB + env 回退） | 04#数据模型 / 04#关键流程 | `apps/api/app/llm.py` / `models.py` |
| 解析/检索/生成失败文案 | 02#三态 / 03 各页失败态 | `apps/web/src/locales/*.js` |
| 中英界面：静态文案前端；接口 message 后端 | 01#约束 / 02#产品层 / 03 视觉约束 / 04#横切 | `apps/web/src/locales` · `apps/api/app/i18n*.py` |
| UI 简约大方（品牌先、少页、控件轻） | 03#设计理念（对齐 `product-ui` skill） | `apps/web/src/App.vue` 等视图 |
| 主路径 Playwright 冒烟 | 04#主路径冒烟（对齐 `product-e2e` skill） | `apps/web/scripts/e2e-smoke.mjs` |
| 前后端同域 `/api`；上传 PUT `/api/storage` | 04#前后端关系 | `apps/web/vite.config.js` / `apps/web/nginx.conf` |
| 前端 HTTP 工程化（httpCurry + 具名 api） | 04#前后端关系 | `apps/web/src/http/` |
